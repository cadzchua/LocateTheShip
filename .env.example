# Copy this file to .env and adjust as needed.
# Everything has a working default, so this step is optional.

# Your aisstream.io API key (get one free at https://aisstream.io).
# If unset, the demo key baked into docker-compose.yml is used.
AISSTREAM_API_KEY=
